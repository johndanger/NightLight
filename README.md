# NightLight

This plugin configures Night Light mode in Dank Material Shell, it allows the user to specify a longitude/latitude for automatic setting of Night Light at sunset and then disable Night Light at sunrise. The plugin also allows a schedule to be set, and a color temperature. All functionality is done via dms ipc calls.